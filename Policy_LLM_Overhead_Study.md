# Policy Generator LLM의 오버헤드와 경량 모델 대체 가능성 — Progent · Conseca 분석

> **목적**: 정책 강제(policy enforcing) 계열 IPI 방어에서 **정책 생성기(policy generator) LLM이 차지하는 실제 비용**을 단계별로 분해하고, 이를 경량 모델로 대체했을 때 방어 성능이 유지되는지 판정
> **대상 논문**: Progent([arXiv:2504.11703](https://arxiv.org/abs/2504.11703)) · Conseca([arXiv:2501.17070](https://arxiv.org/abs/2501.17070), HotOS'25)
> **선행 문서**: [IPI_LLM_Defense_Survey.md](IPI_LLM_Defense_Survey.md) §8(C6 정책 강제), [IPI_Defense_Rebuttals.md](IPI_Defense_Rebuttals.md) §4.1
> **작성일**: 2026-09-08
> **상태**: §1~§5 확정(문헌·코드 근거 확보) / §6~§7 실측 대기

---

## 목차

- [0. 요약](#0-요약)
- [1. 문제 정의 — 병목은 강제기가 아니라 생성기다](#1-문제-정의--병목은-강제기가-아니라-생성기다)
- [2. Progent 오버헤드 분해](#2-progent-오버헤드-분해)
- [3. Conseca 오버헤드 — 논문이 말하지 않은 것과 구현체가 말해주는 것](#3-conseca-오버헤드--논문이-말하지-않은-것과-구현체가-말해주는-것)
- [4. 두 설계의 구조적 비교](#4-두-설계의-구조적-비교)
- [5. 경량 모델 대체 — 이미 발표된 증거](#5-경량-모델-대체--이미-발표된-증거)
- [6. 본 작업의 실측 결과](#6-본-작업의-실측-결과)
- [7. 권고 — 어느 단계에 어느 등급의 모델을 배치할 것인가](#7-권고--어느-단계에-어느-등급의-모델을-배치할-것인가)
- [8. 참고문헌](#8-참고문헌)

---

## 0. 요약

### 결론 먼저

| 질문 | 답 | 근거 |
|---|---|---|
| Policy LLM은 얼마나 비싼가? | **Progent 전체 지연의 66%**(에이전트 본체 4.50s vs 정책 LLM 8.75s). 결정론적 SMT 검사는 0.002s로 사실상 무료 | Progent v1 §5.3 Figure 11 |
| 그중 어디가 비싼가? | **정책 "갱신"이 policy LLM 비용의 82%**(7.20s / 8.75s). 초기 생성은 1.55s에 불과 | 동일 |
| 경량 모델로 바꾸면? | **초기 생성은 대체 가능, 갱신은 붕괴.** gpt-4o-mini는 생성만 쓸 때 ASR 3.18%로 gpt-4o(3.82%)보다 낫지만, 갱신을 켜면 8.11%로 악화. Llama-3.3-70B는 19.39%까지 치솟음 | Progent v1 Appendix B Table 3 |
| Conseca는? | **논문이 오버헤드를 아예 보고하지 않는다.** 대신 Google `gemini-cli`에 실제 구현체가 stable 릴리스로 존재하며, 이미 정책 생성기로 **Flash(경량 티어)를 선택**했다 | 코드 분석 §3.2 |

### 한 문장 요약

> **비용의 82%를 차지하는 단계(정책 갱신)가 정확히 경량 모델이 실패하는 단계다.** 두 축이 정렬돼 있지 않으므로, "정책 모델 전체를 한 등급 낮춘다"는 접근은 실패한다. 대신 **신뢰 컨텍스트만 보는 단계와 비신뢰 데이터를 읽는 단계를 분리해 서로 다른 등급의 모델을 배치**하는 것이 유일하게 열려 있는 절충점이다.

### 이 문서가 새로 밝힌 것

1. Progent의 런타임 분해표는 **v1에만 있고 최신 버전에서는 삭제**됐다. 최신본에는 지연·토큰 수치가 전혀 없다(§2.1).
2. Conseca 논문에는 오버헤드 실측이 없으며, 저자들이 완화책으로 **증류(distillation)와 캐싱을 직접 지목**했다(§3.1).
3. **Google `gemini-cli`가 `security.enableConseca`로 Conseca를 stable v0.56.0부터 탑재**하고 있다. 다만 **논문의 핵심 주장인 "결정론적 강제"가 구현에서는 Flash LLM 판정으로 대체**됐고, **모든 오류 경로가 fail-open**이다(§3.2).
4. Progent 저자와 Conseca 저자가 **모두 "경량 모델 파인튜닝/증류"를 미해결 과제로 명시**했다(§5.3).

---

## 1. 문제 정의 — 병목은 강제기가 아니라 생성기다

선행 조사([IPI_LLM_Defense_Survey.md](IPI_LLM_Defense_Survey.md) §8)에서 정책 강제 계열이 1순위로 평가된 근거는 **결정론성**이었다. LLM 판정기는 오판이 곧 보안 구멍이 되지만, 심볼릭 정책은 로그로 증명 가능하고 감사에 강하다.

그런데 이 계열에는 구조적 딜레마가 있다. **정책을 누가 쓰는가?**

- **사람이 쓴다** → 결정론적이고 빠르지만, 태스크마다 정책을 손으로 만들어야 한다. Progent 논문의 표현대로 *"가능한 모든 사용자 태스크에 대해 단계별 정책을 수동 작성하는 것은 비현실적"*이다.
- **LLM이 쓴다** → 확장 가능하지만, **정책 생성 자체가 LLM 호출**이므로 "LLM 호출을 줄여 지연을 아낀다"는 이 계열의 장점이 상쇄된다.

SoK 재현 실측([arXiv:2511.15203](https://arxiv.org/abs/2511.15203))이 이 딜레마를 정확히 잡아냈다:

| 구성 | ASR | 유틸리티 | 토큰 | 응답시간 |
|---|---|---|---|---|
| 무방어 GPT-4o | — | 80.41% | 8,020 | 4.88s |
| **Progent (수동 정책)** | **0.00%** | 72.62% | **7,788** | **3.88s** |
| **Progent-LLM (자동 정책)** | 5.45% | **77.31%** | 8,925 | **16.91s** |

SoK의 해석도 같다 — *"정책이 사전에 수동 설계되면 프레임워크는 실행 중 정적 정책 판정 시간만 추가되어 시간 오버헤드가 최소다. 그러나 LLM 기반 자동 정책 생성·설계 구성요소가 포함되면 전체 런타임이 상당히 증가한다. Progent-LLM은 시스템 설계·런타임 검사 계열에 필적하는 시간을 소비한다."*

**따라서 이 계열의 상용화 가능성은 "정책 생성기 LLM을 얼마나 싸게 만들 수 있는가"에 달려 있다.** 강제기(SMT/regex)는 이미 충분히 싸다.

---

## 2. Progent 오버헤드 분해

### 2.1 실측 분해표

Progent v1 §5.3 "Runtime Costs"(AgentDojo, 태스크당 평균):

| 구성요소 | 시간 | 비중 | 성격 |
|---|---|---|---|
| 에이전트 본체 | 4.50s | 34.0% | 방어 없이도 발생 |
| **정책 생성 (LLM)** | **1.55s** | 11.7% | 태스크당 1회 |
| **정책 갱신 (LLM)** | **7.20s** | **54.3%** | 툴 호출마다 |
| 정책 검사 (Z3/SMT) | 0.002s | 0.02% | 툴 호출마다, 사실상 무료 |
| **총합** | **13.25s** | 100% | |

- **Policy LLM 합계 = 8.75s = 전체의 66%.** 에이전트 본체 대비 **2.94배**의 추가 지연.
- **생성 : 갱신 = 1 : 4.65.** 논문 표현: *"주된 오버헤드는 정책 생성과 갱신에서 오며, 갱신이 생성보다 4~5배 비싸다."*
- **결정론적 검사는 무료다.** 논문도 *"LLM 정책 관리가 개입하지 않는 사용 사례(ASB, EHRAgent 실험)에서 Progent는 최소한의 런타임 오버헤드만 도입한다"*고 명시.

> ⚠️ **이 표는 v1에만 존재한다.** 최신 arXiv 버전(제목이 *"Progent: Securing AI Agents with Privilege Control"*로 변경됨)에서는 Runtime Costs 절이 삭제됐고, 지연·토큰 수치가 전혀 보고되지 않는다. 오버헤드를 인용하려면 [v1](https://arxiv.org/abs/2504.11703v1)을 지정해야 한다.

### 2.2 왜 갱신이 비싼가 — 호출 구조

Progent §5의 알고리즘을 호출 횟수 관점에서 풀면:

```
태스크 시작
 └─ [LLM #1] 초기 정책 생성          입력: 툴 목록 T + 사용자 질의 o₀     (신뢰)
                                                             ← 태스크당 1회

각 툴 호출 cᵢ 마다
 ├─ [LLM #2] "갱신이 필요한가?" 판정  입력: T + o₀ + cᵢ                  (신뢰)
 │                                    ※ 툴 결과 oᵢ는 의도적으로 제외
 ├─ [LLM #3] 갱신 정책 P′ 생성        입력: T + o₀ + cᵢ + oᵢ + P         (비신뢰 포함)
 │                                    ※ #2가 Yes일 때만
 ├─ [Z3] A(P′) ⊆ A(P) 검사            0.002s. narrowing이면 자동 승인,
 │                                    expansion이면 승인 필요 (전체의 6%)
 └─ 툴 실행
```

**핵심**: LLM #2는 **툴 호출마다 무조건 발생**한다. 갱신이 실제로 필요 없어도 "필요 없다"는 판정을 LLM에게 받아야 한다. 툴 호출이 N회인 태스크의 policy LLM 호출 횟수는 **1 + N + (갱신 필요 횟수)**다. 이것이 7.20s의 정체다.

### 2.3 설계상 중요한 비대칭

LLM #2는 **툴 결과 oᵢ를 입력받지 않는다.** 논문의 설명: *"툴 호출 결과는 이 단계에서 제외되어, 진행 여부 결정이 잠재적 악성 콘텐츠에 노출되지 않고 이루어지도록 보장한다."*

즉 **policy LLM 3개 호출 중 비신뢰 데이터를 읽는 것은 LLM #3 하나뿐**이다. LLM #1과 #2는 신뢰 컨텍스트만 본다. 이 사실은 §5의 경량 모델 결과와 결합될 때 결정적 함의를 갖는다(§7).

### 2.4 안전장치의 비용은 0에 가깝다

Progent의 보안 보장(Monotonic Confinement)은 Z3 SMT 비교로 성립하며, 이 비용은 0.002s다. 승인이 필요한 expansion은 **전체 갱신의 6%**에 불과해 사람 개입 비용도 낮다. **Progent에서 비싼 것은 보안 메커니즘이 아니라 정책을 만드는 LLM이다.**

---

## 3. Conseca 오버헤드 — 논문이 말하지 않은 것과 구현체가 말해주는 것

### 3.1 논문 (HotOS'25 포지션 페이퍼)

| 항목 | 내용 |
|---|---|
| 정책 생성기 모델 | **Gemini 1.5 Pro** (에이전트 플래너도 동일 모델) |
| 호출 시점 | `set_policy(task, trusted_ctxt)` — **사용자 요청당 1회** |
| 갱신 메커니즘 | **없음** |
| 강제 방식 | `is_allowed(cmd, policy)` — 정책의 **정규식 제약을 결정론적으로 평가** |
| 평가 규모 | Debian 리눅스 환경, 20개 태스크, 5회 평균 |
| **오버헤드 보고** | **없음** |

유틸리티 결과(Figure 3, 20개 태스크 중 완료 수):

| 정책 | 완료 태스크 | 부적절 행동 차단 |
|---|---|---|
| 없음 | 14.0 / 20 | ✗ |
| 정적 permissive | 12.2 / 20 | ✗ |
| 정적 restrictive | 0.0 / 20 | ✓ |
| **Conseca** | **12.0 / 20** | **✓** |

오버헤드에 대한 언급은 Discussion의 한 문단이 전부다:

> *"LLM 사용은 정책 생성에 태스크당 오버헤드를 추가하며, 이는 모델 크기에 따라 수 초가 걸릴 수 있다. LLM 효율화 — 예를 들어 더 능력 있는 모델을 증류(distilling)하는 것 — 로 이 비용을 줄일 수 있으며, 품질을 일부 희생하는 트레이드오프가 있을 수 있다. 또는 캐싱 기법을 써서 흔한 컨텍스트에 대한 정책을 사전 생성하거나 동적으로 저장할 수 있다. 마지막으로, LLM 없이 컨텍스트 정책을 만들 수는 없는가?"*

**즉 본 작업의 질문(경량 모델 대체)은 Conseca 저자가 직접 제시한 미해결 과제다.**

### 3.2 실제 구현체 — `gemini-cli`

Google의 `gemini-cli`에 Conseca 구현이 들어 있다. **stable v0.56.0 / v0.57.0 / v0.58.0 모두에 포함**을 확인했다(nightly 전용이 아님).

**활성화**: `~/.gemini/settings.json`

```json
{ "security": { "enableConseca": true } }
```

스키마 정의(`packages/cli/src/config/settingsSchema.ts`) — label *"Enable Context-Aware Security"*, default `false`, 재시작 필요. 설명: *"컨텍스트 인식 보안 검사기를 활성화한다. 이 기능은 LLM을 사용해 프롬프트에 기반한 툴 사용 보안 정책을 동적으로 생성하고 강제하여, 의도치 않은 행동에 대한 추가 보호 계층을 제공한다."*

**구성 파일**:

| 파일 | 역할 |
|---|---|
| `packages/core/src/safety/conseca/conseca.ts` | 싱글턴 체커. 정책을 `activeUserPrompt` 기준으로 캐시 → **프롬프트당 1회 생성** |
| `packages/core/src/safety/conseca/policy-generator.ts` | 정책 생성. 모델 = `DEFAULT_GEMINI_FLASH_MODEL` |
| `packages/core/src/safety/conseca/policy-enforcer.ts` | 정책 강제. 모델 = `DEFAULT_GEMINI_FLASH_MODEL` |
| `packages/core/src/safety/conseca/types.ts` | `ToolPolicy = { permissions, constraints, rationale }` — 논문의 3요소와 일치 |
| `packages/core/src/telemetry/conseca-logger.ts` | `CONSECA_POLICY_GENERATION` / `CONSECA_VERDICT` OTel 이벤트 |
| `packages/core/src/policy/policies/conseca.toml` | 모든 툴(`toolName = "*"`)에 priority 100으로 훅 |

**관찰 1 — 논문의 핵심 주장이 구현에서 뒤집혔다.**

논문 §3.3의 제목은 *"Deterministic Policy Enforcement"*이고, 프로토타입은 정규식 매칭으로 결정론적 평가를 한다. 그러나 `policy-enforcer.ts`의 `enforcePolicy()`는 **Gemini Flash에게 프롬프트를 보내 판정을 받는다**:

```
You are a security enforcement engine. ... Evaluate the tool call against the policy.
Output a JSON object with: "decision": "allow" | "deny" | "ask_user", "reason": ...
```

→ **오버헤드 프로파일이 근본적으로 달라진다.** 논문 설계는 `생성 1회 + 무료 강제 N회`지만, 구현은 `생성 1회 + LLM 강제 N회`다. 툴 호출당 LLM 1회라는 점에서 **Progent의 "갱신 필요?" 판정과 동일한 비용 구조**다.
→ 동시에 **결정론성이라는 이 계열의 핵심 이점이 사라진다.** SoK가 지적한 근본원인 #4("LLM 판정기의 오판이 곧 보안 구멍")가 그대로 적용된다.

**관찰 2 — 모든 오류 경로가 fail-open이다.**

`policy-generator.ts`와 `policy-enforcer.ts` 양쪽에서, 다음 상황이 전부 `SafetyCheckDecision.ALLOW`를 반환한다:

- content generator 미초기화
- 빈 응답
- **JSON 파싱 실패**
- 예외 발생
- 툴 이름 누락
- `conseca.ts`에서 정책이 아직 생성되지 않은 경우 (`'No security policy generated.'`)

→ **경량 모델 실험에서 가장 중요한 리스크 지표다.** 모델을 낮출수록 스키마 위반·파싱 실패가 늘고, 실패는 곧 무방어다. 이 상황에서 측정된 ASR은 "정책이 허용했다"가 아니라 "정책이 없었다"를 섞어 담고 있으므로, **파싱 실패율을 병기하지 않은 ASR 수치는 해석 불가**다.

**관찰 3 — Google은 이미 경량 티어를 선택했다.**

두 파일 모두 Pro가 아니라 `DEFAULT_GEMINI_FLASH_MODEL`(현재 `gemini-2.5-flash`)을 쓴다. 논문 프로토타입이 Gemini 1.5 **Pro**였던 것과 대조된다. 즉 산업계는 "정책 생성기는 경량 모델로 충분하다"에 이미 베팅했지만, **그 베팅을 IPI 벤치마크로 검증한 공개 자료는 없다.**

**관찰 4 — 구조화 출력을 쓴다.**

두 파일 모두 zod 스키마 → `zodToJsonSchema` → Gemini `responseSchema` + `responseMimeType: 'application/json'`으로 **제약 디코딩**을 건다. 이는 경량 모델을 쓰기 위한 필수 조건이며, 후술하듯 Progent 구현체가 갖추지 못한 부분이다(§5.2).

---

## 4. 두 설계의 구조적 비교

| 축 | Progent | Conseca (논문) | Conseca (gemini-cli 구현) |
|---|---|---|---|
| 정책 표현 | JSON Schema (툴명 + 인자 제약) | 정규식 제약 + rationale | 자연어 `constraints` + rationale |
| 생성 시점 | 태스크 시작 | 사용자 요청 시 | 사용자 프롬프트당 1회(캐시) |
| **갱신** | **있음 (툴 호출마다 판정)** | 없음 | 없음 |
| 강제 방식 | **JSON Schema 검증 + Z3 SMT** (결정론) | 정규식 평가 (결정론) | **Flash LLM 판정 (확률적)** |
| 안전 보장 | Monotonic Confinement (SMT 증명) | 없음(신뢰 컨텍스트 격리에 의존) | 없음 |
| **툴 호출당 LLM 호출** | **1~2회** | **0회** | **1회** |
| 태스크당 LLM 호출 | 1 + N + α | **1회** | 1회 (+ 툴 호출당 1회) |
| 실패 시 동작 | 정책 위반 → 차단 | — | **fail-open (허용)** |
| 코드 공개 | ✅ [sunblaze-ucb/progent](https://github.com/sunblaze-ucb/progent) | ❌ | ✅ [google-gemini/gemini-cli](https://github.com/google-gemini/gemini-cli) |

**핵심 트레이드오프**: Conseca는 **갱신을 포기해 태스크당 LLM 1회**로 비용을 눌렀고, Progent는 **갱신을 도입해 정확도를 얻는 대신 툴 호출당 LLM 호출**을 지불했다. Progent 논문의 갱신 효과 측정에 따르면 갱신은 banking 도메인 ASR을 9.72% → 2.78%로 낮춘다. **7.20s를 지불하고 얻는 것이 이 6.94%p다.**

→ 구조적으로 **Conseca ≈ Progent의 `SECAGENT_UPDATE=False` 모드**다. Progent 논문의 "no update" 열이 Conseca 설계가 도달할 수 있는 성능의 근사 상한선이며, 이는 §5의 표에서 직접 읽을 수 있다.

---

## 5. 경량 모델 대체 — 이미 발표된 증거

### 5.1 Progent v1 Appendix B Table 3

AgentDojo overall, 에이전트 LLM은 gpt-4o로 고정, **정책 LLM만 교체**. 무방어 기준선: 유틸(무공격) 79.38% / 유틸(공격 하) 53.42% / **ASR 41.18%**.

| Policy LLM | 갱신 OFF<br>유틸(무공격) | 갱신 OFF<br>유틸(공격) | 갱신 OFF<br>**ASR** | 갱신 ON<br>유틸(무공격) | 갱신 ON<br>유틸(공격) | 갱신 ON<br>**ASR** |
|---|---|---|---|---|---|---|
| gpt-4o | 70.10% | 57.24% | 3.82% | 76.29% | 61.21% | **2.23%** |
| claude-3.7-sonnet | 71.13% | 58.67% | 3.97% | 76.29% | 60.26% | 2.86% |
| o3-mini | 63.92% | 55.33% | 3.18% | 63.92% | 55.65% | 6.68% |
| gpt-4o-mini | 60.82% | 51.67% | **3.18%** | 62.89% | 52.79% | 8.11% |
| Qwen2.5-72B | 60.82% | 51.35% | **2.54%** | 64.95% | 52.14% | 13.83% |
| Llama-3.3-70B | 61.85% | 52.31% | **2.07%** | 64.95% | 50.87% | **19.39%** |

### 5.2 세 가지 해석

**① 초기 정책 생성에서는 경량 모델이 이미 대등하거나 더 낫다.**

갱신 OFF 열의 ASR은 2.07~3.97% 구간에 전부 몰려 있고, **최저값 3개가 모두 경량/오픈 모델**(Llama-3.3-70B 2.07%, Qwen2.5-72B 2.54%, gpt-4o-mini 3.18%)이다. gpt-4o(3.82%)보다 낫다. §2.3에서 본 대로 이 단계는 **신뢰된 사용자 질의만 보므로 적대적 추론이 필요 없고**, 요구되는 것은 "태스크에 필요한 툴만 골라내기"라는 비교적 단순한 능력이다.

**② 갱신에서는 붕괴한다.**

갱신 ON에서 gpt-4o는 3.82% → 2.23%로 개선되지만, 경량 모델은 전부 악화한다:

| 모델 | 갱신으로 인한 ASR 변화 |
|---|---|
| gpt-4o | 3.82% → 2.23% (**−1.59%p**) |
| claude-3.7-sonnet | 3.97% → 2.86% (**−1.11%p**) |
| o3-mini | 3.18% → 6.68% (+3.50%p) |
| gpt-4o-mini | 3.18% → 8.11% (+4.93%p) |
| Qwen2.5-72B | 2.54% → 13.83% (**+11.29%p**) |
| Llama-3.3-70B | 2.07% → 19.39% (**+17.32%p**) |

논문의 진단: *"이는 이들 모델이 최소권한 원칙을 강제할 만한 보안 추론 능력이 부족하거나, 악성 명령을 식별할 안전 메커니즘이 없기 때문일 가능성이 높다. 예를 들어 약한 모델은 갱신 시 잘못된 정보를 사용하거나 더 취약해질 수 있다."*

**갱신 단계는 비신뢰 툴 결과를 읽고 "이 안에서 무엇이 정당한 정보이고 무엇이 주입인가"를 판단해야 한다.** 이것은 IPI 방어 그 자체이며, 경량 모델이 못하는 바로 그 작업이다.

**③ 유틸리티 손실은 별개 문제이며 원인이 규명되지 않았다.**

갱신 OFF 기준 유틸(무공격)은 gpt-4o 70.10% vs 경량 모델 60.82~63.92%로 **일관되게 6~9%p 낮다.** 정책이 과도하게 좁아 정상 행동까지 막는다는 뜻이다.

다만 **이 손실의 상당 부분이 "보안 추론 부족"이 아니라 "출력 형식 오류"일 가능성**이 있다. Progent 구현체(`secagent/tool.py`)는 정책 JSON을 **정규식으로 파싱**(`utils.py:extract_json`)하고, 모델별로 출력 형식 지시문을 하드코딩 분기로 덧붙인다 — `o1`/`o3`/`gpt-4.1`/`claude`/`gpt-4o-mini`/`gemini`/`meta-llama/`/`Qwen/` 각각에 대해 다른 문자열을 붙이는 코드가 `get_SYS_PROMPT()`와 `get_SYS_PROMPT_2()`에 존재한다. 이런 임시방편이 필요했다는 사실 자체가 **경량 모델의 실패 중 상당수가 형식 문제**였음을 시사한다. 대조적으로 gemini-cli의 Conseca는 처음부터 제약 디코딩을 쓴다(§3.2 관찰 4).

→ **따라서 공정한 재측정에는 구조화 출력(structured output / guided decoding)이 필수이며, 파싱 실패율을 독립 지표로 분리해야 한다.** 본 작업의 실험 설계에 이를 반영했다.

### 5.3 두 논문 저자가 남긴 미해결 과제

**Progent** (v1 §6 Discussion, Policy Construction):

> *"최신 LLM은 합리적인 정책을 생성해 공격 성공률을 크게 낮출 수 있다. 그러나 §5.3이 보여주듯 최신 LLM은 추가 런타임 비용을 야기하고, **더 작은 모델은 정책 갱신에 필요한 안전·추론 능력을 갖추지 못할 수 있다.** ... 더 근본적인 해법은 **정책 생성과 갱신을 위해 더 작은 모델을 파인튜닝하여, 방어 효과를 유지하면서 비용을 줄이는 것**이다. 이를 위해 사람이 작성한 정책 데이터셋을 수집하거나 합성 데이터를 생성한 뒤, SFT나 강화학습으로 정책 생성 능력을 향상시킬 수 있다."*

**Conseca** (§7 Discussion): §3.1에 인용 — 증류와 캐싱.

**두 논문이 동일한 방향을 가리킨다.** 본 작업은 그 전 단계, 즉 **"파인튜닝 없이 기성 경량 모델을 어디까지 쓸 수 있는가"의 경계를 먼저 실측**하는 데 해당한다.

### 5.4 미개척 구간

| 구간 | 논문 커버리지 |
|---|---|
| 프론티어 (gpt-4o, claude-sonnet) | ✅ Progent |
| 추론 소형 (o3-mini) | ✅ Progent |
| API 소형 (gpt-4o-mini) | ✅ Progent |
| 오픈 대형 (70B급) | ✅ Progent |
| **오픈 중소형 (7B~32B)** | ❌ **없음** |
| **Flash/Lite 티어** | ❌ 없음 (gemini-cli가 실제로 쓰지만 벤치마크 없음) |
| **단계별 이종 모델 배치** | ❌ **없음** — 논문은 전 단계를 한 모델로 일괄 교체만 시도 |

마지막 항목이 §2.3의 비대칭과 결합될 때 가장 유망하다: **policy LLM 3개 호출 중 비신뢰 데이터를 읽는 것은 1개뿐**이므로, 나머지 2개(초기 생성 + 갱신 게이트)만 경량 모델로 내려도 호출 횟수 기준으로는 대부분을 커버한다.

---

## 6. 본 작업의 실측 결과

> 🚧 **실험 진행 중.** 아래는 측정 예정 항목의 골격이다.

### 6.1 실험 설계 요약

| 항목 | 내용 |
|---|---|
| 하네스 | [sunblaze-ucb/progent](https://github.com/sunblaze-ucb/progent) + AgentDojo |
| 실행 환경 | **로컬 OpenAI 호환 서버(vLLM 등)에 올린 Qwen 7~8B**. 외부 API 호출 없음 |
| 에이전트 LLM | 로컬 Qwen 고정 (`qwen-local`) — 스윕 내내 불변 |
| 정책 LLM 스윕 | 동일 로컬 서버의 모델들. 단계별 라우팅으로 M4 하이브리드 구성 |
| 모드 | M0 무방어 / M1 생성만(≈Conseca) / M2 Auto-Deny / M3 Auto-Approve / **M4 하이브리드** |
| 신규 계측 | 단계별(init / update_gate / update_gen / z3) 지연·토큰·호출 횟수, **정책 JSON 파싱 실패율** |
| 공정성 통제 | JSON 모드 도입, on/off 양쪽 측정 |

> ✅ **이 구성은 §5.4에서 미개척으로 지목한 7B~8B 대역을 정면으로 겨냥한다.** 논문이 다룬 가장 작은 정책 모델은 70B급이었고, 그보다 아래는 어느 논문도 측정하지 않았다. 로컬 서빙이므로 API 비용 없이 반복 실행과 3회 재현이 가능하다는 이점도 있다.
>
> ⚠️ **단, 기준선 대조가 약해진다.** 에이전트 LLM이 gpt-4o가 아니므로 논문의 절대 수치(banking ASR 2.78% 등)와 직접 비교할 수 없다. 대신 **동일 에이전트 위에서의 상대 비교**(M0 무방어 대비 각 모드의 ASR·유틸 변화)가 판정 기준이 된다. 7~8B 에이전트는 무방어 ASR과 기본 유틸리티가 gpt-4o와 다르므로, **M0 무방어 실행을 반드시 먼저 확보해 모든 수치를 그 기준으로 정규화**해야 한다.
>
> ⚠️ **구조화 출력.** `args` 필드는 임의의 JSON Schema를 담으므로 OpenAI strict `json_schema`로는 표현할 수 없다. 로컬 서버에서는 대신 vLLM의 **guided decoding**(`extra_body={"guided_json": ...}`)으로 정책 봉투(`{"policies":[{name, args}]}`)를 디코딩 단계에서 강제한다. 이는 단순 JSON 모드보다 강해 **형태 위반까지 차단**한다 — 7B 모델이 실패하는 주된 지점이 구문이 아니라 형태이므로 이 통제가 결정적이다. 그래도 남는 위반은 `shape_fail`로 집계한다.

**하네스 구현 상태** (`progent/` 하위, 실행 대기):

| 파일 | 내용 |
|---|---|
| `secagent/instrument.py` (신규) | 단계별 모델 라우팅, JSONL 메트릭 싱크, 프로바이더 판별, guided decoding 스키마 |
| `secagent/tool.py` (패치) | `api_request`에 stage 인자·지연·토큰 계측, 3개 호출 지점 stage 태깅, `_parse_policy` 파싱 실패 계측, `check_tool_call` Z3 타이머, 모델 id 접두사 대신 프로바이더 기반 분기 |
| `agentdojo/.../models.py`, `agent_pipeline.py` (패치) | 에이전트 LLM으로 로컬 서버를 쓰는 `qwen-local` / `qwen-local-prompting` 추가 |
| `analysis/summarize_metrics.py` (신규) | Figure 11 형식 집계 + 토큰·재시도·파싱 실패율 |
| `analysis/probe_models.py` (신규) | 스윕 전 로컬 서버·파라미터 호환성 점검 |
| `run_sweep.sh` (신규) | M0~M4 모드 × 정책 모델 실행 |

합성 데이터로 집계 파이프라인을 검증했다 — 논문 비율(init 1.55s + update 7.20s)을 입력하면 태스크당 8.75s가 그대로 재현된다.

패치 중 **업스트림 버그를 하나 발견했다**: `api_request`에서 o1/o3 계열 분기가 `if` 뒤에 이어지는 `if/else`에 의해 덮어써져, **추론 모델은 API를 두 번 호출하고 첫 번째(developer 역할) 결과가 버려진다.** gpt-4o 계열은 영향이 없지만 o3-mini 수치는 이 경로를 탔을 가능성이 있다. `elif`로 수정했으며, 재현 실행 시 원본 동작과 대조가 필요하다.

**M4 하이브리드**가 본 작업의 핵심 가설이다: `init` + `update_gate`는 경량 모델(신뢰 컨텍스트만 봄), `update_gen`만 상위 모델(비신뢰 데이터를 읽음). 로컬 구성에서는 같은 서버에 올린 더 큰 모델이나 별도 엔드포인트를 `HEAVY_MODEL`로 지정한다. §2.3의 구조적 비대칭과 §5.2의 성능 비대칭이 정확히 같은 경계선에서 갈리므로, 이 경계를 따라 모델을 배치하면 비용과 보안을 동시에 취할 수 있다는 예측.

### 6.2 하네스 정합성 검증

> 측정 대기 — `SECAGENT_POLICY_MODEL=gpt-4o-2024-08-06`, M3, banking 스위트에서 논문 값(ASR 2.78%, 유틸 68.06%) 재현 여부

### 6.3 오버헤드 분해 재현

> 측정 대기 — 논문 Figure 11(4.50 / 1.55 / 7.20 / 0.002)과의 대조

### 6.4 정책 모델 스윕 결과

> 측정 대기

### 6.5 M4 하이브리드 결과

> 측정 대기

### 6.6 gemini-cli Conseca 실측

> 측정 대기. 하네스는 준비 완료(실행 절차는 [EXPERIMENT_README.md](EXPERIMENT_README.md)).

비교 구성 4종:

| 구성 | 내용 |
|---|---|
| 기본(출하 상태) | 생성·강제 모두 `gemini-2.5-flash` |
| 정책 모델 경량화 | 생성기만 `gemini-3.1-flash-lite` |
| 강제기 경량화 | 강제기만 `gemini-3.1-flash-lite` |
| **결정론적 강제기** | 논문 §3.3 설계 복원 — 툴 호출당 LLM 1회가 사라진다 |

결정론 변형을 위해 생성기가 `arg_constraints`(인자명 → 정규식)를 추가로 내도록 하고, 강제를 JS에서 평가하는 경로를 구현했다(테스트 9개 포함, 총 29개 통과). **기본값은 출하 상태 그대로**이므로 LLM 강제기 측정이 오염되지 않는다.

구현 과정에서 확인한 설계상의 함의: 결정론 경로는 정책에 없는 툴을 **거부**한다. 출하된 LLM 경로는 같은 상황에서 빈 정책 `{}`를 모델에 넘기고, 모델이 답하지 못하면 허용한다. 즉 §3.2 관찰 2의 fail-open은 오류 처리 방식의 문제일 뿐 아니라 **기본값 자체가 deny가 아니라 allow**라는 더 근본적인 차이다.

---

## 7. 권고 — 어느 단계에 어느 등급의 모델을 배치할 것인가

> 🚧 **§6 완료 후 확정.** 현재까지의 근거로 세울 수 있는 잠정 가설:

1. **정책 강제기는 절대 LLM으로 만들지 말 것.** Progent의 Z3 검사는 0.002s이고 증명 가능하다. gemini-cli의 Conseca 구현은 이를 Flash LLM으로 대체해 툴 호출당 LLM 1회를 지불하면서 결정론성까지 잃었다. 자체 구현 시 반드시 Progent 방식(JSON Schema + SMT)을 따를 것.
2. **초기 정책 생성은 경량 모델로 내릴 수 있다.** 신뢰된 사용자 질의만 보는 단계이며, 발표된 수치상 경량 모델이 프론티어와 대등하거나 낫다. 단 유틸리티 6~9%p 손실을 감안해야 하고, 그 손실의 원인이 형식 문제인지 확인이 필요하다.
3. **정책 갱신은 경량 모델로 내리면 안 된다.** 비신뢰 데이터 판독이 요구되며, ASR이 최대 19.39%까지 악화한다. 대안은 (a) 갱신을 끄고 Conseca식 1회 생성으로 가거나, (b) 갱신만 프론티어 모델에 남기는 것(M4).
4. **어떤 구성을 택하든 fail-open 경로를 봉쇄할 것.** 경량 모델일수록 스키마 위반이 늘고, 파싱 실패가 허용으로 이어지면 방어가 조용히 사라진다. 파싱 실패는 **deny 또는 ask_user**로 처리해야 한다.
5. **구조화 출력은 선택이 아니라 전제다.** 제약 디코딩 없이 측정한 경량 모델 수치는 보안 능력이 아니라 형식 준수 능력을 재는 것에 가깝다.

---

## 8. 참고문헌

| 문헌 | 링크 | 본 문서에서의 역할 |
|---|---|---|
| Progent: Securing AI Agents with Privilege Control | [arXiv:2504.11703](https://arxiv.org/abs/2504.11703) | 최신본 — 설계·보안 보장 |
| Progent **v1** | [arXiv:2504.11703v1](https://arxiv.org/abs/2504.11703v1) | **§5.3 Runtime Costs, Appendix B Table 3 — 오버헤드·경량 모델 수치의 유일한 출처** |
| Progent 구현체 | https://github.com/sunblaze-ucb/progent | `secagent/` — 정책 생성·SMT 비교 |
| Contextual Agent Security (Conseca) | [arXiv:2501.17070](https://arxiv.org/abs/2501.17070) | HotOS'25 포지션 페이퍼 |
| Conseca 구현체 | https://github.com/google-gemini/gemini-cli | `packages/core/src/safety/conseca/` (stable v0.56.0+) |
| Taxonomy, Evaluation and Exploitation of IPI-Centric LLM Agent Defense Frameworks (SoK) | [arXiv:2511.15203](https://arxiv.org/abs/2511.15203) | 통일 조건 재현 실측 (Progent vs Progent-LLM) |
| AgentDojo | [arXiv:2406.13352](https://arxiv.org/abs/2406.13352) | 평가 벤치마크 |
| 선행 조사 | [IPI_LLM_Defense_Survey.md](IPI_LLM_Defense_Survey.md) | C6 정책 강제 카테고리 평가 |
| 반박 정리 | [IPI_Defense_Rebuttals.md](IPI_Defense_Rebuttals.md) | §4.1 Progent의 적용 범위 한계 |
