# Optional local checkout of a patched gemini-cli (see conseca/README.md §7)
gemini-cli/

# Per-task workspaces and telemetry written by conseca/run_task.py
conseca/runs/
__pycache__/
conseca/.venv/
conseca/mcp_results/
conseca/extracted/

# Conseca bridge runtime files (conseca/bridge.sh)
conseca/bridge.log
conseca/.bridge.pid

# run_pilot.sh output when run detached (e.g. ./run_pilot.sh --pilot > pilot.log)
conseca/pilot.log
